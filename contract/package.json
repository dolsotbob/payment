{
  "name": "contracts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testToken": "npx hardhat run scripts/deployTestToken.ts --network kairos",
    "deploy:paymentGateway": "npx hardhat run scripts/deployPaymentGateway.ts --network kairos",
    "deploy:all": "npx hardhat run scripts/deployAll.ts --network kairos",
    "test": "hardhat test",
    "clean": "npx hardhat clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.9",
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0",
    "ethers": "^6.14.4"
  }
}
